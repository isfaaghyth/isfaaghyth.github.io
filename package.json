{
  "name": "isfa-dev",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c 'jekyll serve'",
    "build": "tinacms build && jekyll build"
  },
  "dependencies": {
    "tinacms": "^2.3.1"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.3.1"
  }
}
